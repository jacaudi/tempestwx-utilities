# Git
.git
.gitignore

# Documentation
README.md
*.md
docs/

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml

# Local build artifacts / agent tooling
/tempestwx-utilities
.superpowers/
.claude/

# UI build artifacts / deps -- the Dockerfile's "ui" stage rebuilds these
# fresh from web/package.json + web/src, then overlays the result into the
# builder stage; a stale local web/dist or web/node_modules must never leak
# into the build context.
**/node_modules
web/dist

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.exe
*.dll
*.so
*.dylib

# Test coverage
*.out
coverage.txt

# Dependencies (if using vendor)
# vendor/

# Docker
Dockerfile*
docker-compose*
docker-bake.hcl
.dockerignore

# Temporary files
*.tmp
*.temp
/tmp/